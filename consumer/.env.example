# Base URL of the DEPLOYED API, including the version prefix. Must be the public URL, not
# localhost: the point of this page is to prove the API works from outside its own codebase.
# Example: https://property-listings-api.onrender.com/api/v1
VITE_API_BASE_URL=
